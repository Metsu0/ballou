<?xml version='1.0' encoding='UTF-8' ?>
<odoo>
  <data>
    <record id="view_employee_level_filter" model="ir.ui.view">
      <field name="name">hr.employee.search</field>
      <field name="model">hr.employee</field>
      <field name="arch" type="xml">
        <search string="Employees">
          <field name="name" string="Employees"
                 filter_domain="['|',('work_email','ilike',self),('name','ilike',self)]"/>
          <field name="level_id"/>
          <field name="category_ids" groups="hr.group_hr_user"/>
          <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
          <separator/>
          <filter string="Unread Messages" name="message_needaction" domain="[('message_needaction', '=', True)]"/>
          <group expand="0" string="Group By">
            <filter name="group_manager" string="Manager" domain="[]" context="{'group_by':'parent_id'}"/>
            <filter name="group_coach" string="Coach" domain="[]" context="{'group_by':'coach_id'}"/>
            <filter name="group_department" string="Department" domain="[]" context="{'group_by':'department_id'}"/>
            <filter name="group_job" string="Job" domain="[]" context="{'group_by':'job_id'}"/>
            <filter name="group_company" string="Company" domain="[]" context="{'group_by':'company_id'}"
                    groups="base.group_multi_company"/>
          </group>
        </search>
      </field>
    </record>
    <record id="act_employee_from_level" model="ir.actions.act_window">
      <field name="name">Employees</field>
      <field name="res_model">hr.employee</field>
      <field name="view_mode">kanban,form,tree</field>
      <field name="search_view_id" ref="view_employee_level_filter"/>
      <field name="context">{
        "search_default_level_id": [active_id],
        "default_level_id": active_id}
      </field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          Add a new employee
        </p>
        <p>
          With just a quick glance on the Odoo employee screen, you
          can easily find all the information you need for each person;
          contact data, job position, availability, etc.
        </p>
      </field>
    </record>
    <record id="hr_level_view_kanban" model="ir.ui.view">
      <field name="name">hr.level.kanban</field>
      <field name="model">hr.level</field>
      <field name="arch" type="xml">
        <kanban class="oe_background_grey o_kanban_dashboard o_hr_kanban">
          <field name="name"/>
          <field name="sequence"/>
          <field name="number_of_employees"/>
          <templates>
            <t t-name="kanban-box">
              <div>
                <div t-attf-class="o_kanban_card_header">
                  <div class="o_kanban_card_header_title">
                    <div class="o_primary">
                      <field name="name"/>
                    </div>
                    <div class="o_secondary">
                      <field name="sequence"/>
                    </div>
                  </div>
                  <div class="o_kanban_manage_button_section">
                    <a class="o_kanban_manage_toggle_button" href="#">
                      <i class="fa fa-ellipsis-v" role="img" aria-label="Manage" title="Manage"/>
                    </a>
                  </div>
                </div>
                <div class="container o_kanban_card_content">
                  <div class="row">
                    <div class="col-6 o_kanban_primary_left">
                      <span>
                        <field name="number_of_employees"/>
                      </span>
                      <button class="btn btn-primary" name="%(act_employee_from_level)d" type="action">Employees
                      </button>
                    </div>
                    <div class="col-6 o_kanban_primary_right">
                    </div>
                  </div>
                </div>
                <div class="container o_kanban_card_manage_pane dropdown-menu" role="menu">
                  <div t-if="widget.editable" class="o_kanban_card_manage_settings row">
                    <div class="text-right">
                      <a role="menuitem" type="edit">Settings</a>
                    </div>
                  </div>
                </div>

              </div>
            </t>
          </templates>
        </kanban>
      </field>
    </record>
    <record id="view_employee_level_tree" model="ir.ui.view">
      <field name="name">hr.level.tree</field>
      <field name="model">hr.level</field>
      <field name="arch" type="xml">
        <tree string="Levels">
          <field name="name"/>
          <field name="sequence"/>
        </tree>
      </field>
    </record>
    <record id="view_employee_level_form" model="ir.ui.view">
      <field name="name">hr.level.form</field>
      <field name="model">hr.level</field>
      <field name="arch" type="xml">
        <form string="Level">
          <sheet>
            <group col="4">
              <field name="name"/>
              <field name="sequence"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>
    <record id="view_level_filter" model="ir.ui.view">
      <field name="name">hr.level.search</field>
      <field name="model">hr.level</field>
      <field name="arch" type="xml">
        <search string="Levels">
          <field name="name" string="Levels"/>
          <field name="sequence"/>
        </search>
      </field>
    </record>
  </data>
</odoo>